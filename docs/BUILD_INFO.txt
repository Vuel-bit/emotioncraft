Emotioncraft Psyche MVP
Build: v0_2_96 (from v0_2_95_warncap_enumfix)
Changes: Lobby plan-choice start fix; intake success popup; bottom drawer plan formatting; top bar quirk pills; no-energy SFX error(); zero-pair symmetric cost.
Chunk 3B:
- Removed Total Psyche cap/warning/break system (per-hue cap only).
- Center donut core now shows yellow treatment hold progress (PLAN_CHAIN holdSec) with brief flash on completion.
- Jam break rules updated (Amount High: set jammed Amount=85 + Spin +15 all; Spin Max penalty now +100 to two lowest psyche hues).
- Any mental break pauses the sim and shows an OK modal with well-name adjustments.

Hotfix:
- Fixed _setBreakModal scope crash in systems_breaks.js (missing brace).
- Fixed break snapshots to include amount/spin/psy keys (a/s/psy) so popup lists adjusted wells.
- Fixed break overlay markup placement inside <body> (valid DOM).
- Fixed break overlay update call to run each HUD render (ui_hud.js).

Chunk 3B (Part A UI/mechanics fixes):
- Bottom drawer CSS: current step + next step columns always fit (no off-screen next step).
- PLAN_CHAIN: canonical 10s hold for all steps except SPIN_ZERO; hold only accumulates while satisfied; no extra post-hold delay; brief completion flash then immediate step advance.
- Center core: shows PLAN_CHAIN hold countdown number + ring timing matches canonical 10s rule.
- Zen: unified time limit to 10:00 via T.ZEN_TIME_LIMIT_SEC and added top-right HUD timer.
- Intake congrats: fixed broken hideZenCongrats() + intake modal wiring; updated copy to "{Name} is no longer an intake patient.".
- HTML: fixed missing closing </div> for #breakOverlay.

Chunk 3B (Part B HUD/quirks/debug fixes):
- Top HUD patient header: 2-line layout (Name+Traits line 1, Quirks line 2) with telegraph/active quirk text glow.
- Quirks: enforce global minimum spacing (T.DISP_MIN_GAP_SEC) between scheduled fires + a fire-time safety net to reduce frequent clumping.
- Debug panel: now shows only raw quirk force totals by default; input instrumentation hidden unless ?inputdebug=1.

Build: emotioncraft_v0_2_103_passD_passA33_spillFx_spin3x
Date: 2026-02-19
Notes:
- Visual-only: spin reads ~3× faster (scaled spin visual multipliers + surface directional ripple rotation; no gameplay changes).
- Visual-only: added spill stream FX (ribbon + droplets) between wells; overflow rides the outer lane and underflow rides the inner lane.
- Added runtime-only spill telemetry bucket (SIM._spillFx) for renderer (no mechanics changes).
